version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: btsli
    ports:
      - "5432:5432"
    volumes:
      - btsli_pg:/var/lib/postgresql/data

volumes:
  btsli_pg:
